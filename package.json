{
  "name": "eiken-word-tactics",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-runtime-vocabulary.mjs && node scripts/validate-build.mjs",
    "test": "node --test tests/*.test.mjs",
    "start": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
